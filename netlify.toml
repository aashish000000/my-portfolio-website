[build]
  publish = "public"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# No /* → index.html catch-all: this site is static; that rule can return HTML for asset URLs
# and break PDFs and caching. Real files under public/ are served normally.
